# DataSource Config
spring:
  application:
    name: gateway
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
    gateway:
      routes:
        - id: user-login
          uri: lb://login
          predicates:
              - Path=/user/**
server:
  port: 9527